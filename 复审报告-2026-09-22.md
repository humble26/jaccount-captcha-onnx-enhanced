# jAccount 验证码识别项目 · 复审报告

> 复审时间：2026-09-22 晚
> 复审对象：`E:\harness\jAccount验证码识别-项目归档\`（2026-09-22 19:11 归档）
> 复审方式：**不采信归档结论，全部重新实测**。所有数字均由本次独立脚本跑出（脚本见 `vm_dump/analysis/_audit_*.py`）
> 一句话结论：**总结论（98% 是数据密度边界、生产保持原生模型）成立；但归档报告中有 3 处结论/论证错误，其中一处是把对的改错了。**

---

## 0. 结论速览

| 项 | 状态 |
|---|---|
| 生产版本（油猴 4.5.1 / 扩展 1.0.7） | 无需改动 |
| 线上模型（原生 nn_model.onnx） | 保持，不回写 |
| 归档总结论「98% 无法用训练突破」 | ✅ 经独立复验成立 |
| 归档报告 §4「微调一处未修正」 | ❌ **错误**：实测 6 处被修正 |
| 归档报告 §4「训练集提升只影响概率不影响 argmax」 | ❌ **错误**：argmax 变了 7 张 |
| 归档报告 §3 的论证依据（权重差小 ⇒ 预测同） | ⚠️ 依据不成立，结论正确 |
| 回写验证用 step 25 权重而非 step 300 | ⚠️ 选择偏差，削弱了验证强度 |
| 归档后脚本可复现性 | ❌ 硬编码路径全部失效，需修 |
| 新增可落地优化（换图判据） | ✅ 实测换图率 15% → 6~7%，拦截能力不变 |

> **上表中的 4 项错误、1 项选择偏差与 1 项可复现性缺陷已于 2026-09-22 晚全部修复；
> 换图判据优化也已落地到生产脚本。逐项结果见 §7。**

---

## 1. 项目推进过程（时间线）

### 阶段一 · 9-21：脚本从"跑不起来"到"可观测、可自愈"（16 轮迭代，v4.0.0 → v4.5.1）

- **v4.0.0** 接上被闲置的 ResNet ONNX（原脚本里 `recognizeWithONNX()` 是死代码，实际跑的是 74.2% 的 Tesseract），修正 27 类 + blank@26 判长度、阈值 `>=156`。
- **v4.1.0** 代码审查修 8 处（换图漏识别、旧帧、静默降级…）。
- **v4.2.0** `document-start` + 并行下载，冷启动 12.03 MB / 2.2 s。
- **v4.3.0 / v4.4.0** 灰度取整（106 像素判定差异 → 0）、覆盖逻辑真值表。
- **v4.4.1** 事故修复：4.4.0 重构 `loadScript` 时漏了 `s.src = url;`，导致所有识别失败。
- **v4.4.2 / 4.4.3** 把 N 种失败原因变成 N 种可见信号（横幅带版本号 + 失败环节 + 完整错误）。
- **v4.4.4** ★ 靠横幅一句话锁定真根因：ORT 的 UMD 包装遇到页面上的 `define`/`exports` 就挂不到 `window`，改为受控作用域执行（遮蔽 `define/exports/module/require`）。
- **v4.4.5** 横幅改回文档流内（4.4.3 的 fixed 定位挡住了验证码本身）。
- **v4.5.0 / 4.5.1** 低置信自动换图重试（阈值 0.60 是死阈值 → 0.999），并修掉 6 处语义错误（**填入的答案不属于屏幕上的图**、异步换图在旧图上兑现、AbortError 误判、闸门误伤手动换图…）。

### 阶段二 · 9-22 上午：模型天花板与架构评估

产出一份评估：97.7% 已接近该模型 + 该数据的判别极限，错误 **100% 集中在第 4 字符位**，5 个错例全是视觉近邻混淆（x→o、c→o、y→u、w→g、g→z）。否证了 TTA、11 种预处理变体、ORT 优化等级、冻结特征换头（−41.71 pp）等路径。

### 阶段三 · 9-22 中午至下午：端到端微调研究线（新增的一条独立研究）

链路完整：**采集 300 张 → 自动标注 + 人工核对 → 合并 420 张 → 端到端重训（温和 lr=3e-6 / 300 步）→ 定向增广 75 变体 → 回写 ONNX → 独立复验 → 判定无交付价值**。

工具链从零建起：纯 NumPy ResNet-20 前向/反向（im2col 向量化，数值梯度 657/657 通过）、训练/评估/回写/复验脚本。

### 阶段四 · 9-22 19:11：重新归档

两个交付形态 + 研究线 + 文档，剪切移动到 `E:\harness\jAccount验证码识别-项目归档\`，附归档说明与快照。

---

## 2. 我独立复验了什么（8 项，全部通过）

| # | 复验项 | 方法 | 结果 |
|---|---|---|---|
| 1 | 纯 NumPy 前向 == onnxruntime | `resnet20_np.forward_fast` vs ORT，抽样 180 张 | 预测 0 差异 ✅ |
| 2 | 回写 ONNX vs 原生（step 25 版） | ORT 跑 520 张 | 预测 0 差异 ✅ |
| 3 | 权重差异数字 | 逐张量比对 53 个 initializer | max\|Δ\|=3.627e-05、累计 1.761e-04，**与报告完全一致** ✅ |
| 4 | 图结构一致性 | 逐节点比对 | 56 节点算子序列完全一致，dtype 均 float32×52 + int64×1 ✅ |
| 5 | `filter_trainable` 完备性 | 列 initializer 名单 | 53 → 保留 52，唯一丢弃的是 `/Constant_1_output_0`（Reshape 的 shape 常量），正确 ✅ |
| 6 | 训练/留出集泄漏 | 8 个样本集两两做字节级 + 像素级 md5 | 跨集重复 **0**；`new300` 与 `holdout` 零重叠 ✅ |
| 7 | 两份 300 张拷贝一致性 | `vm_dump/new300` vs 归档 `sampled/new_captchas` | 300/300 逐字节相同 ✅ |
| 8 | 自标注真值质量 | **盲测**：48 张只给序号不给预测，我独立读数后程序比对 | 48/48 一致；另 33 张位4高危样本也全部一致 ✅ |

补充验证：训练轨迹 `meta.json` 与报告 §2.2 表格逐行吻合（step 50/150/300 的 loss 与 acc4）。

---

## 3. 发现的错误

### 3.1 【最重要】归档报告 §4 的"更正"本身是错的

报告 §4 用一张表格宣布：

> | 难例 | 真值 | 原生 | 微调(npz) | 微调(回写ONNX) |
> | c12 | x | 误判 | 仍误判 | 仍误判 |
> | n051 | y | 误判 | 仍误判 | 仍误判 |
> | n088 | c | 误判 | 仍误判 | 仍误判 |
>
> 「训练集 acc 提升来自 softmax 概率边界的微调，对 argmax 预测无影响」

**实测不成立。** 我用**改动最大的 step 300 权重**（`e2e_final_extra.npz`）重新回写 ONNX，跑 520 张：

```
回写(step25  官方那份): 预测变化 0 张   | logits max|Δ| = 6.833e-01
回写(step300 本次)    : 预测变化 7 张   | logits max|Δ| = 5.128e+00

  c12.png  原生=riioo  step300=riixo   ← 真值 x，修正
  n051.png 原生=ffruu  step300=ffryu   ← 真值 y，修正
  n088.png 原生=mwoo   step300=mwoc    ← 真值 c，修正
  c078.png 原生=ijjaa  step300=ijjca   ← 人工真值 ijjca，修正
  c215.png 原生=mxpp   step300=mxpk    ← 人工真值 mxpk，修正
  c269.png 原生=ltfmm  step300=ltfbm   ← 人工真值 ltfbm，修正
  c198.png 原生=niilr  step300=niirr   ← 真值 niilr，退化（经 8 倍放大目视确认第 4 位是 l）
```

**报告说"仍误判"的三例，全部被修正了。** 报告自己 §2.2 的表格其实已经给出线索：step 300 的训练集位4 = **99.76%**（vs 原生 98.57%），420 × 0.12% ≈ 5 张被修正 —— 与实测吻合。

**根因**：`_backport_onnx.py` 第 18 行回写的是 `e2e_final_extra_best_hold4.npz`，即 **step 25** 的权重，不是 step 300 的最终权重。两者改动量差 10 倍：

| | 全局 max\|Δ\| | 逐张量 max\|Δ\| 之和 |
|---|---|---|
| 原生 → step 25 | 3.627e-05 | 1.761e-04 |
| 原生 → step 300 | 3.754e-04 | 1.805e-03 |

step 25 的改动太小，不足以翻转任何 argmax，于是得出"一处未修正"的假象。

**影响**：研究线的**总结论不受影响**（含 step 300 在内，7 张变化样本全部落在训练集，**留出集 100 张 0 变化**，98% 仍未被突破）；但报告里那段"向依赖该结论的后续判断致歉"是把**正确结论改成了错误结论**——这是一次过度自我更正。

### 3.2 报告 §3 的论证依据不成立（结论正确）

报告称"微调权重 ≈ 原生（累计差 1.8e-4）→ 回写后预测相同 → 无交付价值"，把"预测相同"归因于"权重几乎没变"。

实测：step 25 回写后 **logits 差中位 0.455 / 最大 0.683**；且用 float64 纯 NumPy 重跑，差异同样是 0.455 —— 说明这不是浮点噪声，是权重更新经 21 层放大的真实输出位移（logits 幅度约 46，即 1%）。`_rt_compare_result.txt` 里其实也记着 0.876~0.920，但报告未提及。

**预测之所以没翻转，是因为决策余量远大于输出扰动**（置信度 0.99999 意味着正确类与次优类的 logit 间隔常在 9 以上）。

**为什么这重要**：不能用"权重差小所以安全"去论证任何更大的改动。（本次实测也证明了这一点：step 300 的权重差是 step 25 的 10 倍，就翻转了 7 张。）

### 3.3 回写验证存在选择偏差

用**改动最小**的 step 25 权重做"回写无价值"的验证，是反向的选择。保守做法应当用改动最大的最终权重。结论（留出集未突破）不受影响，但验证强度被削弱了。

### 3.4 `_autolabel.py` 未处理 blank 类（潜在缺陷，本轮无实害）

```python
for pos in range(5):
    pi = int(np.argmax(logits[pos][0]))
    chars.append(chr(ord('a') + pi))      # 第 5 头 index 26 = blank -> 会产出 '{'
...
minconf = min(confs) if len(pred) == 5 else confs[0]   # len(pred) 恒为 5，else 是死代码
```

- 300 张里 **160 张**的原始 `pred` 末尾带 `{`（这些是 4 位码）；
- `_apply_labels.py` 用 `clean()`（截末尾 `{`）打了补丁，因为 blank 只可能出现在第 5 位，补丁功能上有效；
- 但 `minconf` 把 **blank 位的置信也算进 min**，而生产脚本是 `if (best >= CFG.numClasses) continue;`（blank 不入统计）——**口径不一致**；
- 实测后果：**0 张样本被误分类**（blank 位置信同样高于 0.999），故为潜在缺陷。

---

## 4. 代码卫生与方法学问题清单

| # | 位置 | 问题 | 影响 |
|---|---|---|---|
| 1 | `train_e2e.py` `cmd_gen` | 读 `h['acc4']`，而 hist 写入的 key 是 `acc4_train` | 该子命令一调用就 KeyError（因未使用而未暴露） |
| 2 | `train_e2e.py` `report()` | `lab = targets  # placeholder` 死代码；`load_split` 里有 `for d in (...): pass` 空循环 | 无功能影响 |
| 3 | `_apply_labels.py` | `"purple" if False else "pos4_hot"` 编辑残留 | 无功能影响 |
| 4 | 归档内全部脚本 | 硬编码 `E:\harness\重构研究\...`（4 条路径已确认全部不存在） | **归档不可直接复现**：`train_e2e.py` 的 `NEW_SRC`、`_autolabel.py` 的 `sys.path`、`_prep_new300.py` 的 `SRC` 全部失效 |
| 5 | `train_e2e.py` `--eval-every` | 用留出集选模型并保存 `best_hold4`，留出集实际变成了验证集 | 报告未声明。方向保守（挑最优也只有 98%），结论未被高估 |
| 6 | 真值文件 | `new300_gt.json` 未并入 `ground_truth_all.json` | 无法直接做 520 张统一评估，需手动合并 |
| 7 | 标注文档 | 报告称"三重交叉验证"，但其中"ResNet 复跑"是同一模型、Tesseract 基线仅 74% | 有效独立验证实际只有人工目测；本次盲测补充了 48 张的证据 |

---

## 5. 优化方向

### 5.1 【推荐 · 可立即落地】换图判据从"最小 softmax 置信"改为"logit 决策间隔"

**动机**：softmax 概率受 logit 整体尺度影响（温度效应），而 logit 间隔（top1 − top2）是决策边界距离的直接度量。现状 `lowConfidence: 0.999` 让约 **15% 的图触发换图**，用户体感是"脚本老在换图"。

**实测**（判据替换后，两集都必须拦下该集全部错误才算合格）：

| 策略 | 调参集 120 自动化率 | 拦错 | 误伤 | 留出集 100 自动化率 | 拦错 | 误伤 |
|---|---|---|---|---|---|---|
| 现行 min conf ≥ 0.999 | 80.8% | 3/3 | 20 | 89.0% | 2/2 | 9 |
| 候选A 第4位 conf ≥ 0.999 | 91.7% | 3/3 | 7 | 94.0% | 2/2 | 4 |
| **候选C min margin ≥ 6** | **93.3%** | 3/3 | **5** | **94.0%** | 2/2 | **4** |

换图率（用户体感）：

| 数据组 | 现行 | 候选A（第4位） | 候选C（margin<6） |
|---|---|---|---|
| 220 张人工标注 | 15.5% | 7.3% | **6.4%** |
| 300 张新增 | 14.0% | 5.7% | 7.0% |

**结论：换图率从约 15% 降到约 6~7%，而拦截错误的能力不变（两集全拦）。**

实现（3 处小改）：

```js
// 1) postprocess 里顺带算最小间隔
let best = 0, bestVal = -Infinity, secondVal = -Infinity;
for (let i = 0; i < n; i++) {
    if (data[i] > bestVal) { secondVal = bestVal; bestVal = data[i]; best = i; }
    else if (data[i] > secondVal) { secondVal = data[i]; }
}
if (best >= CFG.numClasses) continue;        // blank：与置信度一样不参与
marginMin = Math.min(marginMin, bestVal - secondVal);
...
return { text, confidences, minConfidence: minP, minMargin: marginMin };

// 2) CFG 增加
lowMargin: 6,          // logit 间隔下限（替代 lowConfidence 作为主判据）

// 3) recognize 里替换判据
const isLow = (r) => r && r.minMargin < CFG.lowMargin;
```

**取舍说明**：
- 候选C 不依赖"错误只在第 4 位"这一先验，更稳健；候选A 只改一行（`r.confidences[3] < 0.999`），但依赖该先验。
- 两者在 220 / 300 张上的换图率互有胜负（6.4% vs 7.3%；7.0% vs 5.7%），差异在噪声量级。
- **样本量限制**：错误样本只有 5 个，阈值 6 是在这 5 个上选的，有过拟合风险；两个独立集都通过，但阈值应在未来数据上重新标定。
- 换判据后需同步改提示文案（"置信度偏低" → "决策余量不足"），状态条显示的数字也应改为 margin 或两者并列。

### 5.2 修正归档报告（文档层面）

1. 撤销 §4 的错误更正，恢复"微调修正了训练集 6 例"的结论，并注明回写用的是 step 25；
2. 补充 logits 差异数据（0.46/0.68；step 300 为 5.13）与"决策余量"解释，替换"权重差小"的论证；
3. 声明留出集被用于模型选择；
4. 首次实现标注脚本的 blank 处理与置信度口径统一。

### 5.3 修复归档可用性

- 脚本路径参数化（相对路径或单一配置项）；
- 合并 `new300_gt.json` 与 `ground_truth_all.json` → 统一的 520 张评估集（这是"保留资产"最有价值的形态：任何未来改动都能一键回归）；
- 修 `cmd_gen` 的 KeyError、清理死代码。

### 5.4 数据侧：不建议继续加数据

w→g 与 g→z 在 520 张里各只出现 1 次（出现率约 0.2%）。要期望命中 20 个同类难例需采集约 **1 万张**，而收益仅是修正 2 个边缘字形。投入产出比极低，维持报告结论。

### 5.5 一个已被关闭的方向（本次新验证）

我注意到模型 `AveragePool` 的 kernel 是 `(10, 25)`，而 layer3 输出特征图是 `10×28` —— **最右 3 列（对应原图 x≈100-110）从未参与识别**，看起来像结构瑕疵。

实测 520 张的逐列笔画分布：

- `x=0-9`（保留）：520/520 张有笔画，中位 54 px —— 字形并不居中，第一个字符贴着左边缘；
- `x=100-109`（被丢弃）：仅 84/520 张有零星像素，中位 **0**，每列平均 ≤1.0 px。

**结论：被丢弃的确实是空白边距，该结构瑕疵无实害，不必改。** 方向关闭，记录以免将来重复探究。

---

## 6. 我在本次复审中犯的错（一并记录）

1. **第一次盲测联络表把文件名与真值印在图上**，我读小号文字标签时发生错位（把别的样本的真值读成当前样本的），差点把一个正确真值当成"自标注错误"。改为"图上只印大号序号 + 程序化配对"后重测，问题消失。
2. **#27（c031.png）我第一次读成 `clef`**，与真值 `iclef` 不符，一度以为是自标注把模型误判写成了真值。8 倍放大后确认图上第一个字符是 `i`（有点有竖）——**是我漏读，真值正确**。
3. 最初怀疑"297 张自标注真值必然把模型误判洗白，因此'训练集无难例'是循环论证"。48 张盲测（含 24 张最难的位4高危样本）+ 33 张位4全量核对均零错误，**该怀疑基本不成立**；但 48 张给出的错误率上界仍较宽（< 6.25%），不能完全排除。

---

## 7. 本轮已落地的修复（2026-09-22 晚）

### 7.1 生产脚本：换图判据改为「logit 决策间隔」（油猴 4.5.2 / 扩展 1.0.8）

| 项 | 内容 |
|---|---|
| 判据 | `min softmax 置信 < 0.999` → **`min logit 间隔 < 6`**（`CFG.lowMargin: 6`） |
| 实现 | `postprocess()` 一次扫描取 top1/top2，返回 `minMargin`；`isLow` 改用间隔；Tesseract 路径置 `minMargin: Infinity`（不参与换图） |
| 文案 | 「置信度偏低（xx%）」→「决策余量不足（间隔 x.xx）」；`lowConfidence` 保留但仅用于显示与日志 |
| 实测 | 调参集 120 自动化率 80.8% → **93.3%**（误伤 20 → 5）；留出集 100 89.0% → **94.0%**（误伤 9 → 4）；220 张换图率 15.5% → **6.4%**；300 张 14.0% → **7.0%**；tune / hold 两集仍全部拦下各自错误 |
| 落地复验 | 导出 520 张 logits 喂给**从交付脚本抽取的真实 `postprocess`**：text 0 差异、minMargin 最大差 4.77e-7、两版之间逐张一致 |
| 诚实的召回数字 | 520 张整体是**拦 7/8**（漏 c269，margin 6.78）。阈值 7 可全拦，但 tune 段换图率升到 20%（比旧判据还高）；取 6 = 用"漏 1 个边缘错例"换"换图率减半"。详见 `CHANGELOG.md` |
| 未采用 | 加 conf 兜底（`margin<6 或 conf<0.99`）实测与原方案**完全相同** —— 两信号 rank 相关 0.979，兜底不增加拦截，徒增维护成本 |

复验：`bug_hunt_flow` 16/16、`bug_hunt_v2` 9/9、`bug_hunt_retry` 15/15、`race_retry_observer` 7/7、
`test_retry` 8/8、`consistency_check` **25/25**（新增 3 项判据锁）、`final_check` 26/26、
`web_verify` 220/220（与 Python 参考一致，logits 最大差 1.001e-5）、`verify_extension` 220/220（97.7%）、
`node --check` 两版通过。

> 测试侧的一个改动值得一提：`bug_hunt_flow` / `bug_hunt_v2` 没有逐个去改 20 多处测试桩，
> 而是在「桩 → 被测代码」的桥接处加了一层映射（`conf < 0.999 → margin 3`，否则 `→ margin 12`），
> 使既有用例的语义自动保持不变。判据换代时这是最省且最不容易改错的做法。

### 7.2 归档报告的 3 处错误已修正

- `_E2E_FINETUNE_ROUND2_ARCHIVE.md`
  - **§4 撤销**了此前那条错误更正，恢复「微调确实修正了训练集 6 例」并注明回写用的是 step 25；
  - **§3.1 新增**：用 logits 差异（0.455 / 0.683；step 300 为 5.128）+「决策余量」替换原来的
    "权重差小所以预测相同"论证；
  - **§2.2 新增方法学声明**：留出集被用于模型选择（`--eval-every` + `best_hold4`）；
  - §1 结论速览表与 §7 一句话总结同步改写。
- `_backport_onnx.py`：补上"默认回写的是早停权重、会得出误导性结论"的警示，
  并把回写源改为可用 `E2E_NPZ` 环境变量指定。

### 7.3 研究线代码缺陷已修，归档恢复可复现

| 缺陷 | 处理 |
|---|---|
| `_autolabel.py` 未处理第 5 头 blank（160/300 张的 pred 带 `{`） | 源头按生产语义处理（`argmax>=26` 视为 blank）；**复跑验证**：blank 泄漏 160 → 0，长度分布 4:160/5:140，与「旧数据 clean 后」**逐张一致（0 差异）** |
| 同脚本 `minconf` 把 blank 位置信也算进 min（与生产口径不一致） | 只统计实际字符位；复跑确认无样本跨过 0.999 阈值（此前的判断"潜在缺陷、无实害"得到验证） |
| `train_e2e.py` `cmd_gen` 读 `h['acc4']` 导致 KeyError | 兼容 `acc4_train`，已实跑输出 12 行训练历史 |
| `train_e2e.py` / `_apply_labels.py` 的死代码 | 清掉空循环、`lab = targets # placeholder`、`"purple" if False else ...` |
| 归档内 15 个脚本共 36 处硬编码 `E:\harness\重构研究\...` | **批量改写**为基于 `__file__` 的相对定位；改后全部 `.py` 语法通过，并实跑 `_qa_c215.py` 验证可用 |

> 批量改写时踩了一个坑，记下来：注入辅助变量的判断最初写成 `'_ARCH_HERE' not in src`，
> 而替换后的文本里**已经出现** `_ARCH_HERE`（作为表达式的一部分），于是被误判成"已注入"，
> 15 个文件带着未定义变量通过了 `ast.parse`（语法检查不查名字解析），运行时才 NameError。
> **必须检查"定义"（`'_ARCH_HERE = '`）而不是"出现"。**

### 7.4 统一 520 张回归集已建立

新增两个工具（归档 `02-重构研究/analysis/`）：

- `build_unified_gt.py` → 产出 `_sampling_out/unified_gt_520.json`
  （220 张人工标注 + 300 张已核对样本，含分段标记与相对路径索引）；
- `eval_unified.py` → 任意 ONNX 模型在 520 张上的一键评估。

原生模型基线（实跑）：

| 分段 | 张数 | 整串对 | 准确率 | 长度同时对 |
|---|---|---|---|---|
| tune | 120 | 117 | 97.50% | 120 |
| hold | 100 | 98 | 98.00% | 100 |
| new | 300 | 297 | 99.00%（自洽性，见下） | 300 |
| **合计** | **520** | **512** | **98.46%** | **520** |

⚠ 脚本内置了提醒：**new 段的准确率是自洽性指标**（真值由该模型自标注），不能当泛化准确率引用。
另外这一跑也再次印证了「错误 100% 集中在第 4 字符位」—— 8 个误判样本全部落在第 4 位。

---



## 附：本次复审新增脚本

| 脚本 | 用途 |
|---|---|
| `_audit_labels.py` | 复核 300 张标注数据的来源构成、blank 泄漏、口径差异 |
| `_audit_leak.py` | 8 个样本集两两做字节级 + 像素级去重 |
| `_audit_e2e_onnx.py` | 回写 vs 原生预测对比 + NumPy/ORT 前向一致性 |
| `_audit_weight_diff.py` | 逐张量权重差异（复核报告数字） |
| `_audit_logit_gap.py` | 解释"权重差小却 logits 差大"，区分权重效应与浮点噪声 |
| `_audit_backport300.py` | **用 step 300 权重回写并与官方 step 25 版本对照（关键实验）** |
| `_audit_backport.py` | 拷贝一致性 + 回写脚本溯源 |
| `_audit_blind2.py` | 盲测联络表生成 + 读数比对 |
| `_audit_margin.py` / `_audit_margin2.py` / `_audit_margin3.py` | 换图判据对比与分集验证 |
| `_audit_edge.py` | 逐列笔画分布，关闭"池化窗口丢弃信息"方向 |
| `probe_state.py` / `probe_archive.py` | 项目状态与归档结构盘点 |
